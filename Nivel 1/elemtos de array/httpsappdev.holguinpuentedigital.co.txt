#include <iostream>

#include <iostream>

int main() {
    int numeros[5] = {10, 20, 30, 40, 50};

    std::cout << "Elementos del array: " << std::endl;
    for (int i = 0; i < 5; ++i) {
        std::cout << numeros[i] << std::endl;
    }
    return 0;
}